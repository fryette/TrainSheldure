<micro:CaliburnApplication x:Class="TrainShedule_HubVersion.App"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:micro="using:Caliburn.Micro">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.ThemeDictionaries>
                <ResourceDictionary x:Key="Default">
                    <ImageBrush x:Key="HubBackgroundImageBrush" ImageSource="Assets/HubBackground.png" />
                </ResourceDictionary>
                <ResourceDictionary x:Key="HighContrast">
                    <ImageBrush x:Key="HubBackgroundImageBrush" ImageSource="{x:Null}" />
                </ResourceDictionary>
            </ResourceDictionary.ThemeDictionaries>
            <Thickness x:Key="PhoneBorderThickness">2.5</Thickness>
            <FontFamily x:Key="PhoneFontFamilyNormal">Segoe WP</FontFamily>
            <FontWeight x:Key="PhoneButtonFontWeight">Semibold</FontWeight>
            <x:Double x:Key="TextStyleLargeFontSize">18.14</x:Double>
            <Thickness x:Key="PhoneButtonContentPadding">9.5,0,9.5,3.5</Thickness>
            <x:Double x:Key="PhoneButtonMinHeight">0</x:Double>
            <x:Double x:Key="PhoneButtonMinWidth">0</x:Double>
            <Thickness x:Key="PhoneTouchTargetOverhang">0,0</Thickness>
            <SolidColorBrush x:Key="ButtonDisabledBackgroundThemeBrush" Color="Transparent" />
            <Style x:Key="BasedTextBlockStyle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="16" />
                <Setter Property="FontWeight" Value="Bold"></Setter>
                <Setter Property="HorizontalAlignment" Value="Right" />
            </Style>
            <Style x:Key="StopPointTextBlockStyle" TargetType="TextBlock" BasedOn="{StaticResource BasedTextBlockStyle}">
                <Setter Property="FontSize" Value="14" />
            </Style>
            <Style x:Key="ScheduleTextBlockStyle" TargetType="TextBlock" BasedOn="{StaticResource BasedTextBlockStyle}">
                <Setter Property="MaxWidth" Value="400" />
                <Setter Property="Foreground" Value="DarkRed" />
            </Style>
            <Style x:Key="SelectButtonStyle" TargetType="Button">
                <Setter Property="Height" Value="155" />
                <Setter Property="Width" Value="155" />
                <Setter Property="Margin" Value="10,0,50,0" />
            </Style>
            <Style x:Key="RoundByttonStyle" TargetType="Button">
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="Width" Value="40" />
                <Setter Property="Height" Value="40" />
                <Setter Property="BorderBrush" Value="{ThemeResource PhoneForegroundBrush}" />
                <Setter Property="Foreground" Value="{ThemeResource PhoneForegroundBrush}" />
                <Setter Property="BorderThickness" Value="{ThemeResource PhoneBorderThickness}" />
                <Setter Property="FontFamily" Value="{ThemeResource PhoneFontFamilyNormal}" />
                <Setter Property="FontWeight" Value="{ThemeResource PhoneButtonFontWeight}" />
                <Setter Property="FontSize" Value="{ThemeResource TextStyleLargeFontSize}" />
                <Setter Property="Padding" Value="{ThemeResource PhoneButtonContentPadding}" />
                <Setter Property="MinHeight" Value="{ThemeResource PhoneButtonMinHeight}" />
                <Setter Property="MinWidth" Value="{ThemeResource PhoneButtonMinWidth}" />
                <Setter Property="micro:Message.Attach" Value="[Event Click] = [Action Swap]" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Grid x:Name="Grid" Background="Transparent">
                                <VisualStateManager.VisualStateGroups>
                                    <VisualStateGroup x:Name="CommonStates">
                                        <VisualStateGroup.Transitions>
                                            <VisualTransition From="Pressed" To="PointerOver">
                                                <Storyboard>
                                                    <PointerUpThemeAnimation Storyboard.TargetName="Grid" />
                                                </Storyboard>
                                            </VisualTransition>
                                            <VisualTransition From="PointerOver" To="Normal">
                                                <Storyboard>
                                                    <PointerUpThemeAnimation Storyboard.TargetName="Grid" />
                                                </Storyboard>
                                            </VisualTransition>
                                            <VisualTransition From="Pressed" To="Normal">
                                                <Storyboard>
                                                    <PointerUpThemeAnimation Storyboard.TargetName="Grid" />
                                                </Storyboard>
                                            </VisualTransition>
                                        </VisualStateGroup.Transitions>
                                        <VisualState x:Name="Normal" />
                                        <VisualState x:Name="PointerOver" />
                                        <VisualState x:Name="Pressed">
                                            <Storyboard>
                                                <PointerDownThemeAnimation Storyboard.TargetName="Grid" />
                                                <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="Foreground"
                                                        Storyboard.TargetName="ContentPresenter">
                                                    <DiscreteObjectKeyFrame KeyTime="0"
                                                            Value="{ThemeResource ButtonPressedForegroundThemeBrush}" />
                                                </ObjectAnimationUsingKeyFrames>
                                                <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="Background"
                                                        Storyboard.TargetName="Border">
                                                    <DiscreteObjectKeyFrame KeyTime="0"
                                                            Value="{ThemeResource ButtonPressedBackgroundThemeBrush}" />
                                                </ObjectAnimationUsingKeyFrames>
                                            </Storyboard>
                                        </VisualState>
                                        <VisualState x:Name="Disabled">
                                            <Storyboard>
                                                <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="Foreground"
                                                        Storyboard.TargetName="ContentPresenter">
                                                    <DiscreteObjectKeyFrame KeyTime="0"
                                                            Value="{ThemeResource ButtonDisabledForegroundThemeBrush}" />
                                                </ObjectAnimationUsingKeyFrames>
                                                <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="BorderBrush"
                                                        Storyboard.TargetName="Border">
                                                    <DiscreteObjectKeyFrame KeyTime="0"
                                                            Value="{ThemeResource ButtonDisabledBorderThemeBrush}" />
                                                </ObjectAnimationUsingKeyFrames>
                                                <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="Background"
                                                        Storyboard.TargetName="Border">
                                                    <DiscreteObjectKeyFrame KeyTime="0"
                                                            Value="{ThemeResource ButtonDisabledBackgroundThemeBrush}" />
                                                </ObjectAnimationUsingKeyFrames>
                                            </Storyboard>
                                        </VisualState>
                                    </VisualStateGroup>
                                </VisualStateManager.VisualStateGroups>
                                <Border x:Name="Border" BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        Background="{TemplateBinding Background}"
                                        Margin="{ThemeResource PhoneTouchTargetOverhang}" CornerRadius="72">
                                    <ContentPresenter x:Name="ContentPresenter"
                                            AutomationProperties.AccessibilityView="Raw"
                                            ContentTemplate="{TemplateBinding ContentTemplate}"
                                            Content="{TemplateBinding Content}"
                                            Foreground="{TemplateBinding Foreground}"
                                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                            Margin="{TemplateBinding Padding}"
                                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                                </Border>
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
            <Style x:Key="AutoSuggestBoxStyle" TargetType="AutoSuggestBox">
                <Setter Property="IsSuggestionListOpen" Value="True" />
            </Style>
            <Style x:Key="DatePickerStyle" TargetType="DatePicker">
                <Setter Property="MonthFormat" Value="{}{month.abbreviated}" />
                <Setter Property="YearFormat" Value="{}{year.abbreviated}" />
                <Setter Property="DayFormat" Value="{}{day.integer} {dayofweek.abbreviated}" />
            </Style>
            <Style x:Key="StackPanelStyle" TargetType="StackPanel">
                <Setter Property="Orientation" Value="Horizontal" />
                <Setter Property="Background" Value="#FFFF5D00" />
            </Style>
            <Style x:Key="SearchBlockStyle" TargetType="TextBlock">
                <Setter Property="VerticalAlignment" Value="Center" />
                <Setter Property="Height" Value="45" />
                <Setter Property="FontWeight" Value="Bold" />
                <Setter Property="FontSize" Value="25" />
                <Setter Property="Padding" Value="50,0,0,0" />
            </Style>
            <DataTemplate x:Key="ScheduleTemplate">
                <StackPanel Orientation="Horizontal" Background="#FF969696" Width="600" Margin="0,5,0,0">
                    <StackPanel Orientation="Vertical">
                        <Image Source="{Binding ImagePath}" Height="99" Width="50" />
                    </StackPanel>
                    <StackPanel Orientation="Vertical">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding City}" FontSize="14" Foreground="WhiteSmoke" Margin="5,0,0,0"
                                    Style="{StaticResource ScheduleTextBlockStyle}" />
                        </StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <StackPanel Orientation="Vertical" HorizontalAlignment="Right" MinWidth="200" MaxWidth="400">
                                <TextBlock Text="{Binding BeforeDepartureTime}"
                                        Style="{StaticResource ScheduleTextBlockStyle}" />
                                <TextBlock Text="{Binding OnTheWay}" Style="{StaticResource ScheduleTextBlockStyle}" />
                                <TextBlock x:Uid="AddInformation" Foreground="WhiteSmoke"
                                        Style="{StaticResource ScheduleTextBlockStyle}" />
                            </StackPanel>
                            <StackPanel Orientation="Vertical" HorizontalAlignment="Right" MinWidth="120">
                                <TextBlock Text="{Binding StartTime}" FontSize="32" Foreground="White"
                                        Style="{StaticResource ScheduleTextBlockStyle}" />
                                <TextBlock Text="{Binding EndTime}" FontSize="24" Foreground="DimGray" Margin="0,-5,0,0"
                                        Style="{StaticResource ScheduleTextBlockStyle}" />
                            </StackPanel>
                        </StackPanel>
                    </StackPanel>
                </StackPanel>
            </DataTemplate>
            <DataTemplate x:Key="LastScheduleTemplate">
                <ListView ItemsSource="{Binding Trains}" IsItemClickEnabled="True"
                        micro:Message.Attach="[Event ItemClick] = [Action ClickItem($clickeditem)]">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" MinWidth="600" Background="#FF969696" Margin="0,5,0,0">
                                <StackPanel Orientation="Vertical">
                                    <Image Source="{Binding ImagePath}" Height="99" Width="50" />
                                </StackPanel>
                                <StackPanel Orientation="Vertical">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding City}" FontSize="12" MaxWidth="400"
                                                Foreground="WhiteSmoke" Margin="5,0,0,0"
                                                Style="{StaticResource BasedTextBlockStyle}" />
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal">
                                        <StackPanel Orientation="Vertical" HorizontalAlignment="Left" MinWidth="100"
                                                MaxWidth="400" Margin="5,0,0,0">
                                            <TextBlock Text="{Binding BeforeDepartureTime}" Foreground="DarkRed"
                                                    Style="{StaticResource BasedTextBlockStyle}" />
                                            <TextBlock Text="{Binding OnTheWay}"
                                                    Style="{StaticResource BasedTextBlockStyle}" />
                                            <TextBlock x:Uid="AddInformation" Foreground="WhiteSmoke"
                                                    Style="{StaticResource BasedTextBlockStyle}" />
                                        </StackPanel>
                                        <StackPanel Orientation="Vertical" HorizontalAlignment="Right" MinWidth="100">
                                            <TextBlock Text="{Binding StartTime}" FontSize="32" Foreground="White"
                                                    Style="{StaticResource BasedTextBlockStyle}" />
                                            <TextBlock Text="{Binding EndTime}" FontSize="24" Foreground="DimGray"
                                                    Margin="0,-5,0,0" Style="{StaticResource BasedTextBlockStyle}" />
                                        </StackPanel>
                                    </StackPanel>
                                </StackPanel>
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </DataTemplate>
            <DataTemplate x:Key="MainPageTemplate">
                <GridView ItemsSource="{Binding Menu}" IsItemClickEnabled="True" Margin="0,9.5,0,0"
                        ItemTemplate="{StaticResource Standard200X180TileItemTemplate}"
                        micro:Message.Attach="[Event ItemClick] = [Action GoToSearch($clickeditem)]">
                    <GridView.ItemsPanel>
                        <ItemsPanelTemplate>
                            <ItemsWrapGrid />
                        </ItemsPanelTemplate>
                    </GridView.ItemsPanel>
                </GridView>
            </DataTemplate>
            <DataTemplate x:Key="InformationTemplate">
                <StackPanel Orientation="Horizontal" MinWidth="600" Background="#FFFF8000" Margin="0,5,0,0">
                    <StackPanel Orientation="Vertical" Margin="5,5,0,0">
                        <TextBlock Text="{Binding Name}" HorizontalAlignment="Left" VerticalAlignment="Bottom"
                                FontSize="18" MinWidth="150" />
                    </StackPanel>
                    <StackPanel Orientation="Vertical" MinWidth="100">
                        <TextBlock Text="{Binding Place}" FontWeight="Normal"
                                Style="{StaticResource BasedTextBlockStyle}" />
                        <TextBlock Text="{Binding Price}" FontWeight="Normal"
                                Style="{StaticResource BasedTextBlockStyle}" />
                    </StackPanel>
                </StackPanel>
            </DataTemplate>
            <DataTemplate x:Key="StopPointTemplate">
                <StackPanel Orientation="Horizontal" MinWidth="600" Background="#FFFF3A00" Margin="0,2,0,0">
                    <StackPanel Orientation="Vertical" Width="220" Margin="5,0,0,0">
                        <TextBlock Text="{Binding Name}" MaxWidth="300" HorizontalAlignment="Left"
                                VerticalAlignment="Bottom" Style="{StaticResource BasedTextBlockStyle}" />
                    </StackPanel>
                    <StackPanel Orientation="Vertical" MinWidth="150">
                        <TextBlock Text="{Binding Arrivals}" Style="{StaticResource StopPointTextBlockStyle}" />
                        <TextBlock Text="{Binding Stay}" Style="{StaticResource StopPointTextBlockStyle}" />
                        <TextBlock Text="{Binding Departures}" Style="{StaticResource StopPointTextBlockStyle}" />
                    </StackPanel>
                </StackPanel>
            </DataTemplate>
            <DataTemplate x:Key="LastRequestTemplate">
                <StackPanel Orientation="Horizontal" MinWidth="400" MinHeight="40" Background="#FFEB3C00"
                        Margin="5,0,5,5">
                    <TextBlock Text="{Binding From}" FontWeight="Bold" Margin="5,0"
                            Style="{StaticResource BodyTextBlockStyle}" />
                    <TextBlock Text="-" FontWeight="Bold" Margin="5,0" Style="{StaticResource BodyTextBlockStyle}" />
                    <TextBlock Text="{Binding To}" FontWeight="Bold" Style="{StaticResource BodyTextBlockStyle}" />
                </StackPanel>
            </DataTemplate>
            <DataTemplate x:Key="HubSectionHeaderTemplate">
                <TextBlock Margin="0,0,0,-9.5" />
            </DataTemplate>
            <DataTemplate x:Key="Standard200X180TileItemTemplate">
                <Grid Margin="0,0,9.5,9.5" Background="{ThemeResource ListViewItemPlaceholderBackgroundThemeBrush}">
                    <Image Source="{Binding ImagePath}" AutomationProperties.Name="{Binding Title}" Height="138.5"
                            Width="138.5" Stretch="UniformToFill" />
                    <TextBlock Text="{Binding Title}" VerticalAlignment="Bottom" Visibility="Collapsed"
                            Margin="9.5,0,0,6.5" Style="{ThemeResource BaseTextBlockStyle}" />
                </Grid>
            </DataTemplate>
        </ResourceDictionary>
    </Application.Resources>
</micro:CaliburnApplication>