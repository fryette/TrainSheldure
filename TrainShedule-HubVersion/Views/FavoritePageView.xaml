<Page x:Class="TrainShedule_HubVersion.Views.FavoritePageView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:micro="using:Caliburn.Micro"
      xmlns:interactivity="using:Microsoft.Xaml.Interactivity"
      xmlns:core="using:Microsoft.Xaml.Interactions.Core"
      xmlns:infrastructure="using:TrainShedule_HubVersion.Infrastructure"
      xmlns:viewModels="using:TrainShedule_HubVersion.ViewModels"
      mc:Ignorable="d"
      Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
      d:DataContext="{d:DesignInstance viewModels:FavoritePageViewModel}">

    <Grid>
        <ListView x:Name="FavoriteRequests"
                  ItemsSource="{Binding FavoriteRequests}"
                  IsItemClickEnabled="True"
                  Margin="5,0,0,0"
                  ItemTemplate="{StaticResource FavoriteRequestTemplate}"
                  micro:Message.Attach="[Event ItemClick] = [Action SelectItem($clickeditem)]">
        </ListView>
    </Grid>
</Page>
