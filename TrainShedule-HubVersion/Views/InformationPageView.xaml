<Page x:Class="Trains.App.Views.InformationPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:micro="using:Caliburn.Micro"
      mc:Ignorable="d"
      xmlns:viewModels1="using:Trains.App.ViewModels"
      Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
      d:DataContext="{d:DesignInstance viewModels1:InformationPageViewModel}">
    
    <Grid>
        <ListView ItemsSource="{Binding AdditionalInformation}"
                  Margin="5,0"
                  ItemTemplate="{StaticResource InformationTemplate}">
        </ListView>
        <ProgressBar IsIndeterminate="True"
                     VerticalAlignment="Top"
                     Visibility="{Binding IsTaskRun,Converter={StaticResource BooleanToVisibilityConverter}}" />
    </Grid>
    <Page.BottomAppBar>
        <CommandBar>
            <AppBarButton Label="Остановки"
                          Icon="Bookmarks"
                          micro:Message.Attach="[Event Click] = [Action SearchStopPoint]" />
        </CommandBar>
    </Page.BottomAppBar>
</Page>