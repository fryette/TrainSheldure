<Page x:Class="Trains.App.Views.SplashScreenView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:viewModels="using:Trains.App.ViewModels"
      mc:Ignorable="d"
      Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
      d:DataContext="{d:DesignInstance viewModels:SplashScreenViewModel}">

    <Grid Background="{StaticResource SplashScreenColor}">
        <StackPanel>
            <Image Source="/Assets/MainImage.png" />
            <TextBlock x:Uid="DownloadData" />
            <ProgressBar  Value="{Binding Progress}"
                          VerticalAlignment="Bottom"
                          Margin="0,0,0,100" />
        </StackPanel>
    </Grid>
</Page>