<views:MvxWindowsPage x:Class="Trains.WP.Views.SettingsView"
                      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                      xmlns:views="using:Cirrious.MvvmCross.WindowsCommon.Views"
                      xmlns:viewModels="using:Trains.Core.ViewModels"
                      mc:Ignorable="d"
                      Background="{StaticResource BackgroundColorBrush}"
                      RequestedTheme="Dark"
                      d:DataContext="{d:DesignInstance viewModels:SettingsViewModel}">

    <Grid Margin="5,0">
        <Grid>
            <StackPanel>
                <ComboBox Header="Выберите язык"
                          ItemsSource="{Binding Languages}"
                          SelectedItem="{Binding SelectedLanguage,Mode=TwoWay}"
                          SelectedValue="1"
                          Margin="10,0">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Name}" />
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>
                <Button Content="Сбросить настройки"
                        HorizontalAlignment="Stretch" 
                        Command="{Binding SaveChangesCommand}"/>
            </StackPanel>
        </Grid>
        <Grid Visibility="{Binding SaveRun,Converter={StaticResource BooleanToVisibilityConverter}}"
              VerticalAlignment="Center"
              HorizontalAlignment="Center">
            <TextBlock Text="Загружаем данные" />
            <ProgressBar IsIndeterminate="True"
                         Margin="0,15,0,0" />
        </Grid>
    </Grid>
    <Page.BottomAppBar>
        <CommandBar>
            <AppBarButton Label="Сохранить"
                          Icon="Save"
                          Command="{Binding SaveChangesCommand}" />
        </CommandBar>
    </Page.BottomAppBar>
</views:MvxWindowsPage>
